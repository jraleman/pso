import React from 'react';
import { render } from '@testing-library/react';
import SandboxCalculators from '../../containers/SandboxCalculators';

it('renders SandboxCalculators', () => {
  render(<SandboxCalculators />);
});
